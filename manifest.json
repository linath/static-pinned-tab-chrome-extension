{
  "name": "Static Pinned Tabs",
  "description": "Causes links from a pinned tab to open in a new page if the href host doesn't match, keeping the pinned tab on the same page.",
  "version": "1.2",
  "permissions": [
    "activeTab",
    "http://*/",
    "https://*/",
    "webRequest",
    "webRequestBlocking"
  ],
  "background": {
    "scripts": ["background.js"]
  },
  "manifest_version": 2
}
